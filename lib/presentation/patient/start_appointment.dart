import 'package:flutter/material.dart';

class StartAppointmentPage extends StatelessWidget {
  const StartAppointmentPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
